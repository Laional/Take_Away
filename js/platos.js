$(document).ready(function() {
	// Inicializamos la funcion para el PUSHPIN.
	pushpinUp();
	// Inicializamos las funcones de MODAL.
	$('.modal').modal();
	// Inicializamos la funcion para mostrar todos los platos en el INDEX.
	mostrarTodosPlatosIndex();
});
